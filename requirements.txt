# ChatMCP Voice MCP Server
# Core (MCP protocol)
# No dependencies needed for the bridge skeleton

# MiniCPM-o (when model loading is added)
# torch>=2.0.0
# transformers>=4.36.0
# cosyvoice>=0.1.0

# Development
pytest>=7.0.0
